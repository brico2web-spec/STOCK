# نقل Catalog التجاري إلى STOCK

## الملفات الرئيسية

`stock.html` هو لوحة المخزون. حسابات `manager` و`admin` تبقى فيها.

`catalog.html` هو الواجهة التجارية الكاملة لحساب `viewer`. بعد تسجيل الدخول بحساب viewer، يتم التوجيه إليها تلقائياً.

`catalog-style.css` و`catalog-app.js` هما ملفات القالب والمنطق التجاري المنقولين من Catalog.

`catalog-seed.js` يحتوي نسخة البيانات المرفقة: المنتجات والكليان والطلبيات والسلة، ويتم استعمالها فقط إذا لم تكن بيانات Catalog موجودة في المتصفح.

`catalog-assets/loading-intro.mp4` هو فيديو البداية.

## الرفع إلى GitHub

ارفع الملفات الموجودة داخل هذا المجلد إلى جذر مستودع `STOCK`. يجب أن تظهر الملفات الرئيسية مباشرة في الجذر، وأن يبقى `catalog-assets` مجلداً.

## الصلاحيات

حساب `viewer` يتوجه إلى Catalog التجاري. حسابا `manager` و`admin` يبقيان في لوحة المخزون.

## البيانات المنقولة

النسخة المرفقة تحتوي على 79 منتوجاً، 500 كليان، وكوموند واحدة. فواتير الشركة وسجل المستخلاصات المستقلان لا يظهران في نسخ JSON المتوفرة، لذلك لا يمكن نقل بياناتهما الفعلية دون نسخة احتياطية كاملة أحدث من Catalog.

## ملاحظة التخزين

Catalog الأصلي يستعمل localStorage؛ البيانات تبقى مرتبطة بالمتصفح والجهاز. إذا أردت مشاركة الزبناء والطلبيات بين أجهزة متعددة، يجب ترحيل هذه البيانات إلى Supabase في مرحلة لاحقة.
