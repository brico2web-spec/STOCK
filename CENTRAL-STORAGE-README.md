# التخزين المركزي لبيانات Commercial

## ما الذي تغير؟

النسخة v95 تضيف طبقة تخزين مركزية لبيانات Commercial. كل البيانات الموجودة حالياً في LocalStorage تُرفع إلى جدول `commercial_cloud_data` في Supabase، ويُربط كل سجل بمعرّف حساب Supabase. من بعد ذلك، عند الدخول من هاتف أو حاسوب آخر بنفس الحساب، تُجلب نفس البيانات تلقائياً.

تشمل المزامنة الزبناء، السلة، الطلبيات، الطلبيات المكتملة، الأقساط، الإرجاعات، تغييرات الأثمنة، التخفيضات، الأقساط اليومية، الأثمنة، وطلبات فواتير الشركة.

## الإعداد مرة واحدة

1. افتح Supabase ثم **SQL Editor**.
2. افتح الملف `CENTRAL-STORAGE-SETUP.sql` الموجود داخل الحزمة.
3. انسخ الكود كاملاً، الصقه في SQL Editor، ثم اضغط **Run**.
4. ارفع محتويات `STOCK-v95-central-commercial-data.zip` كاملة إلى GitHub Pages، مع الحفاظ على الملف `commercial-cloud-sync.js` وملف `CENTRAL-STORAGE-SETUP.sql`.
5. افتح الموقع بالرابط الجديد مع تحديث النسخة:

```text
https://brico2web-spec.github.io/STOCK/?v=20260817-95
```

## أول دخول بعد التحديث

افتح Commercial من الجهاز الذي يحتوي على البيانات القديمة، وسجّل الدخول بالحساب نفسه. ستُرفع البيانات القديمة تلقائياً إلى التخزين المركزي. قد يعاد تحميل الصفحة مرة واحدة بعد اكتمال المزامنة.

بعد ذلك افتح الموقع من جهاز آخر وسجّل الدخول بالحساب نفسه. ستظهر بيانات ذلك Commercial. حساب Commercial آخر سيحصل على بياناته فقط، ولن يرى بيانات الحساب الأول.

> لا تحذف LocalStorage في الهاتف القديم حتى تتأكد من ظهور جميع البيانات في الجهاز الجديد. النسخة المحلية تبقى مؤقتاً كاحتياط، بينما تصبح Supabase هي المصدر المركزي.

## ملاحظة مهمة

المخزون المشترك يبقى منفصلاً عن بيانات Commercial. رئيس المخزن يحتفظ بصلاحياته ومخزونه المشترك، وكل Commercial يحتفظ بزبنائه وطلبياته وأرشيفه حسب حسابه.
