(() => {
  const $=id=>document.getElementById(id),cfg=window.STOCK_CONFIG||{},cartKey='3d_peintures_new_cart_v1',clientsKey='3d_peintures_clients_v1',pricesKey='3d_peintures_commercial_prices_v1';
  let products=[],cart=JSON.parse(localStorage.getItem(cartKey)||'[]'),selected=null,catalogProducts=[],activeCategory='الكل',editingPrice=null;
  const money=n=>Number(n||0).toLocaleString('fr-FR',{minimumFractionDigits:2,maximumFractionDigits:2}),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])),available=p=>Number(p.total_pieces||0)>Number(p.minimum_stock||0);
  function toast(t){$('toast').textContent=t;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2200)}
  function openPanel(id){document.querySelectorAll('.side-panel').forEach(x=>x.classList.remove('open'));$(id).classList.add('open');$('shade').hidden=false}
  function closePanels(){$('.side-panel')?.classList.remove('open');document.querySelectorAll('.side-panel').forEach(x=>x.classList.remove('open'));$('shade').hidden=true}
  function prices(){try{return JSON.parse(localStorage.getItem(pricesKey)||'{}')}catch(e){return {}}}
  function priceFor(p){const overrides=prices(),key=String(p.product_code||'').trim().toLowerCase(),old=catalogProducts.find(x=>String(x.code||'').trim().toLowerCase()===key);return Number(overrides[key]?.price ?? old?.price ?? old?.priceTiers?.[0]?.price ?? 0)}
  function categories(){return ['الكل',...new Set(products.map(p=>String(p.category||'PRODUITS').trim()).filter(Boolean))]}
  function renderCategoryButtons(){const box=$('categoryButtons');box.innerHTML=categories().map(c=>`<button class="category-btn ${activeCategory===c?'active':''}" data-category="${esc(c)}">${esc(c)}</button>`).join('');box.querySelectorAll('[data-category]').forEach(btn=>btn.onclick=()=>{activeCategory=btn.dataset.category;renderCategoryButtons();renderProducts()})}
  function renderProducts(){const q=String($('status').dataset.query||'').toLowerCase(),list=products.filter(p=>(activeCategory==='الكل'||String(p.category||'PRODUITS')===activeCategory)&&`${p.product_name} ${p.product_code}`.toLowerCase().includes(q));$('productsGrid').innerHTML=list.map(p=>{const ok=available(p),image=p.image_url||'';return `<article class="product-card" data-id="${esc(p.id)}"><div class="product-image"><span class="availability ${ok?'':'off'}">${ok?'متوفر':'غير متوفر حاليا'}</span>${image?`<img src="${esc(image)}" alt="${esc(p.product_name)}">`:'<div class="no-image">3D</div>'}</div><div class="product-info"><div class="product-code">${esc(p.category||'PRODUITS')} · ${esc(p.product_code||'')}</div><h3>${esc(p.product_name)}</h3><div class="product-price">${money(priceFor(p))} <small>درهم / قطعة</small></div><div class="product-meta">${Number(p.pieces_per_box)||0} قطعة في العلبة</div><button type="button" class="price-edit-btn" data-edit-price="${esc(p.id)}">تعديل الثمن</button></div></article>`}).join('');$('empty').hidden=!!list.length;$('status').textContent=`${list.length} منتوج`;document.querySelectorAll('.product-card').forEach(card=>card.onclick=e=>{if(e.target.closest('[data-edit-price]'))return;openProduct(card.dataset.id)});document.querySelectorAll('[data-edit-price]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();openPriceModal(btn.dataset.editPrice)})}
  function openProduct(id){selected=products.find(p=>String(p.id)===String(id));if(!selected)return;const ok=available(selected);$('modalImage').src=selected.image_url||'';$('modalImage').style.display=selected.image_url?'block':'none';$('modalCategory').textContent=selected.category||'PRODUITS';$('modalName').textContent=selected.product_name||'';$('modalCode').textContent=`Code : ${selected.product_code||'—'}`;$('modalStatus').textContent=ok?'متوفر':'غير متوفر حاليا';$('modalStatus').style.color=ok?'#087443':'#b42318';$('modalPrice').textContent=`${money(priceFor(selected))} درهم`;$('modalPerBox').textContent=`${Number(selected.pieces_per_box)||0} قطعة في العلبة`;$('modalBoxes').value=1;$('modalPieces').value=Number(selected.pieces_per_box)||1;updateTotal();$('productModal').hidden=false}
  function updateTotal(){if(selected)$('modalTotal').textContent=`المجموع: ${money((Number($('modalPieces').value)||0)*priceFor(selected))} درهم`}
  function saveCart(){localStorage.setItem(cartKey,JSON.stringify(cart));renderCart()}
  function addSelected(){if(!selected||!available(selected)){toast('المنتوج غير متوفر حاليا');return}const pieces=Math.max(1,Number($('modalPieces').value)||1),row=cart.find(x=>String(x.id)===String(selected.id));if(row)row.pieces+=pieces;else cart.push({id:selected.id,pieces});saveCart();$('productModal').hidden=true;toast('تزاد المنتوج للسلة')}
  function changeCartQuantity(id,d){const r=cart.find(x=>String(x.id)===String(id));if(!r)return;r.pieces=Math.max(0,Number(r.pieces||0)+d);cart=cart.filter(x=>x.pieces>0);saveCart()}
  function renderCart(){let total=0;$('cartItems').innerHTML=cart.map(r=>{const p=products.find(x=>String(x.id)===String(r.id));if(!p)return '';const line=r.pieces*priceFor(p);total+=line;return `<div class="cart-row"><img src="${esc(p.image_url||'')}" alt=""><div class="cart-details"><h4>${esc(p.product_name)}</h4><small>${r.pieces} قطعة · ${money(line)} درهم</small></div><div class="cart-quantity"><button class="quantity-btn" data-cart-minus="${esc(r.id)}" type="button">−</button><strong>${r.pieces}</strong><button class="quantity-btn" data-cart-plus="${esc(r.id)}" type="button">+</button></div></div>`}).join('')||'<div class="empty">السلة خاوية.</div>';$('cartTotal').textContent=`${money(total)} درهم`;$('cartBadge').textContent=cart.reduce((s,x)=>s+Number(x.pieces||0),0)}
  function getClients(){try{return JSON.parse(localStorage.getItem(clientsKey)||'[]')}catch(e){return []}}
  function setClients(v){localStorage.setItem(clientsKey,JSON.stringify(v));renderCustomers()}
  function renderCustomers(){const q=String($('customerSearch').value||'').toLowerCase(),list=getClients().map((c,i)=>({...c,_i:i})).filter(c=>JSON.stringify(c).toLowerCase().includes(q));$('customersList').innerHTML=list.map(c=>`<div class="customer-row"><div><b>${esc(c.name||c.client||c.fullName||'زبون')}</b><div>${esc(c.city||'')} ${esc(c.phone||c.tel||'')}</div></div><div class="customer-actions"><button data-edit-client="${c._i}">تعديل</button><button data-delete-client="${c._i}">حذف</button></div></div>`).join('')||'<div class="empty">مازال ما تسجل حتى زبون.</div>';document.querySelectorAll('[data-edit-client]').forEach(b=>b.onclick=()=>editClient(Number(b.dataset.editClient)));document.querySelectorAll('[data-delete-client]').forEach(b=>b.onclick=()=>deleteClient(Number(b.dataset.deleteClient)))}
  function editClient(i){const list=getClients(),c=list[i]||{},name=prompt('اسم الزبون',c.name||c.client||'');if(name===null)return;c.name=name;const city=prompt('المدينة',c.city||'');if(city!==null)c.city=city;const phone=prompt('الهاتف',c.phone||c.tel||'');if(phone!==null)c.phone=phone;setClients(list);toast('تعدلات معلومات الزبون')}
  function deleteClient(i){if(!confirm('واش متأكد من حذف الزبون؟'))return;const list=getClients();list.splice(i,1);setClients(list);toast('تحذف الزبون')}
  function importCustomers(file){if(!file)return;const reader=new FileReader();reader.onload=e=>{try{const wb=window.XLSX.read(e.target.result,{type:'array'}),rows=window.XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:''}),mapped=rows.map(r=>({name:r.name||r.Name||r['اسم الزبون']||r.client||r.Client||'',city:r.city||r.City||r['المدينة']||'',phone:r.phone||r.Phone||r['الهاتف']||r.tel||''})).filter(x=>x.name);setClients(getClients().concat(mapped));toast(`تزاد ${mapped.length} زبون`)}catch(err){toast('ملف Excel غير صالح')}};reader.readAsArrayBuffer(file)}
  function renderOrders(){let orders=[];try{orders=JSON.parse(localStorage.getItem('3d_peintures_orders_v1')||'[]')}catch(e){}$('ordersList').innerHTML=orders.map((o,i)=>`<div class="customer-row"><b>طلبية ${esc(o.orderCode||i+1)}</b><div>${esc(o.client||'بدون زبون')} · ${money(o.total||0)} درهم</div></div>`).join('')||'<div class="empty">مازال ما تسجل حتى طلبية.</div>'}
  function openPriceModal(id){editingPrice=products.find(p=>String(p.id)===String(id));if(!editingPrice)return;const v=prices()[String(editingPrice.product_code||'').trim().toLowerCase()]||{};$('priceProductName').textContent=`تعديل ثمن: ${editingPrice.product_name}`;$('newProductPrice').value=v.price??priceFor(editingPrice);$('bulkProductPrice').value=v.bulk??'';$('priceModal').hidden=false}
  function savePrice(){if(!editingPrice)return;const all=prices(),key=String(editingPrice.product_code||'').trim().toLowerCase();all[key]={price:Number($('newProductPrice').value)||0,bulk:Number($('bulkProductPrice').value)||0,updatedAt:new Date().toISOString()};localStorage.setItem(pricesKey,JSON.stringify(all));$('priceModal').hidden=true;renderProducts();toast('تحفظ الثمن والتخفيض')}
  async function loadCatalogPrices(){try{const seed=window.CATALOG_SEED;if(seed?.products)catalogProducts=seed.products}catch(e){}}
  async function loadProducts(){if(!window.supabase||!cfg.url||!cfg.anonKey){$('status').textContent='إعدادات المخزون ناقصة';return}const client=window.supabase.createClient(cfg.url,cfg.anonKey),r=await client.from('stock_products').select('id,product_name,product_code,category,pieces_per_box,total_pieces,minimum_stock,image_url,updated_at').order('product_name');if(r.error){$('status').textContent='تعذر تحميل المنتوجات';toast(r.error.message);return}products=r.data||[];renderCategoryButtons();renderProducts();renderCart()}
  $('cartBtn').onclick=()=>{renderCart();openPanel('cartPanel')};$('customersBtn').onclick=()=>{renderCustomers();openPanel('customersPanel')};$('menuBtn').onclick=()=>openPanel('menuPanel');$('menuCustomersBtn').onclick=()=>{closePanels();renderCustomers();openPanel('customersPanel')};$('menuOrdersBtn').onclick=()=>{closePanels();renderOrders();openPanel('ordersPanel')};$('editProductsBtn').onclick=()=>location.href='stock.html#products';$('shade').onclick=closePanels;$('clearCart').onclick=()=>{cart=[];saveCart()};$('saveOrder').onclick=()=>toast(cart.length?'تحفظات الطلبية فهاد الجهاز':'السلة خاوية');$('refreshBtn').onclick=loadProducts;$('customerSearch').oninput=renderCustomers;$('addCustomer').onclick=()=>editClient(getClients().length);$('importCustomers').onclick=()=>$('customersExcel').click();$('customersExcel').onchange=e=>importCustomers(e.target.files[0]);$('addToCart').onclick=addSelected;$('saveProductPrice').onclick=savePrice;$('modalBoxes').oninput=()=>{$('modalPieces').value=(Number($('modalBoxes').value)||0)*(Number(selected?.pieces_per_box)||1);updateTotal()};$('modalPieces').oninput=updateTotal;document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>{if(b.dataset.close==='productModal'||b.dataset.close==='priceModal')$(b.dataset.close).hidden=true;else closePanels()});document.addEventListener('click',e=>{const p=e.target.closest('[data-cart-plus]'),m=e.target.closest('[data-cart-minus]');if(p)changeCartQuantity(p.dataset.cartPlus,1);if(m)changeCartQuantity(m.dataset.cartMinus,-1)});
  loadCatalogPrices().then(loadProducts);setInterval(loadProducts,30000);
})();
