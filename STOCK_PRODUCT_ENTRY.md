# إضافة المنتجات من داخل موقع STOCK

بعد رفع النسخة الجديدة، يظهر زر `＋ إضافة منتوج` للمستخدمين الذين عندهم الدور `manager` أو `admin`. المستخدم `viewer` لا يظهر له الزر.

اضغط الزر، ثم املأ اسم المنتوج والقسم والكمية الإجمالية الموجودة. يمكن كذلك إدخال كود المنتوج، الوحدة، عدد العلب، القطع داخل كل علبة، الحد الأدنى للتنبيه، مكان التخزين، والمورد.

إذا كانت الكمية الإجمالية مكتوبة في خانة `الكمية الإجمالية الموجودة` فسيستعملها الموقع مباشرة. وإذا بقيت الكمية الإجمالية بصفر، يحسب الموقع الكمية من `عدد العلب × القطع في كل علبة`.

بعد الحفظ، يدخل المنتوج إلى جدول `stock_products` وتظهر البطاقة عند جميع المستخدمين المتصلين بقاعدة Supabase. لا يحتاج رئيس المخزن إلى فتح Supabase أو استعمال SQL لإضافة كل منتوج.
