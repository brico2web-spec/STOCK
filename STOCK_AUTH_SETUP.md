# إعداد الدخول الجديد لموقع المخزون

تم تغيير الواجهة لتستعمل **اسم المستخدم + الكود السري** بدل البريد الإلكتروني في الواجهة.

## طريقة إنشاء الحساب

زر **طلب تسجيل جديد** لا ينشئ حساباً مباشراً للعامة؛ يجهز معلومات المستخدم للمشاركة مع المدير، حتى يبقى المخزون محمياً.

بعد توصل المدير بالطلب، يدخل إلى Supabase ثم Authentication ثم Users ثم Add user. في خانة البريد الإلكتروني يكتب اسم المستخدم بهذه الصيغة:

```text
اسم_المستخدم@stock.local
```

مثال:

```text
magasin1@stock.local
```

وفي خانة كلمة المرور يضع الكود السري الذي سيستعمله المستخدم في صفحة الدخول. يمكن تعطيل طلب تأكيد البريد الإلكتروني لأن العنوان داخلي وليس بريداً حقيقياً.

من بعد، يضيف المدير السجل المطابق في جدول `stock_profiles` باستعمال نفس User ID، ويحدد `full_name` و`role` بقيمة `manager` أو `viewer`. حساب `manager` يقدر يسجل حركات المخزون، أما `viewer` فيقدر يشوف فقط حسب سياسات قاعدة البيانات الموجودة.

## ملاحظة مهمة

اسم المستخدم لا يتحول إلى بريد حقيقي؛ الموقع يحوله داخلياً إلى `username@stock.local` فقط لكي يشتغل مع Supabase Authentication بدون إظهار البريد للمستخدم.
