# واجهة المشاهدة الجديدة

المستخدم الذي عنده الدور `viewer` كيشوف واجهة مبسطة بلا الإحصائيات والبحث والتبويبات. كيبانو غير بطاقات المنتجات، جوج بطاقات فكل صف على الهاتف، وكل بطاقة فيها صورة مصغرة واضحة، اسم المنتج، الكود، الكمية، وحالة `متوفر` أو `غير موجد حاليا`.

زر `☰ الأقسام` كيفتح قائمة جانبية كتحتوي على جميع الأقسام، وعلى فلتر `غير المتوفر حاليا` مع عدد المنتجات المنتهية. اختيار قسم أو الفلتر كيغلق القائمة ويعرض المنتجات المطلوبة فقط.

المستخدم `manager` أو `admin` كيبقى عنده عرض التحكم، وتحت كل صورة كيبانو فقط زرا `دخول السلعة` و`خروج السلعة`، إضافة إلى زر `إضافة منتوج` الموجود في النسخة السابقة.

## ملفات ضرورية في GitHub

خاص ترفع الملفات التالية في جذر مستودع STOCK:

```text
stock.html
stock-image-map.js
stock-assets/
```

داخل `stock-assets/` خاص تكون صور `.webp`. ما تحذفش `stock-image-map.js` لأنه كيربط كود كل منتوج بالصورة ديالو.
