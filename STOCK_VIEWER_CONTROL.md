# واجهة المشاهد وواجهة التحكم في STOCK

## المشاهد `viewer`

المشاهد كيشوف واجهة خفيفة فيها صورة كل منتوج، الاسم، الكود، والكمية الداخلة للمخزن. إذا كانت الكمية صفر كيبان فوق الصورة باللون الأحمر: `غير موجد حاليا`. ما كيبانوش أزرار دخول أو خروج السلعة للمشاهد.

## رئيس المخزن أو المدير `manager/admin`

المسؤول كيشوف نفس صور المنتجات والكمية، وتحت كل منتوج كيبانو غير جوج أزرار:

- `＋ دخول السلعة`
- `− خروج السلعة`

كل عملية كتستعمل دالة `record_stock_movement` الحالية، وكتبان مباشرة للمستخدمين بسبب Realtime.

## ملفات الصور

مجلد `stock-assets` ضروري يترفع إلى جذر مستودع GitHub بجانب `stock.html`، وكذلك `stock-image-map.js`. إذا ترفع `stock.html` وحده غادي تبان البطاقات ولكن الصور ما غاديش تتحمل.
