# QA Repeat Notes

تم فتح الملفات الجديدة محلياً بعد إعادة تطبيق التعديلات. ظهر حقل البحث الرئيسي بشكل دائري بحدود ذهبية، ولم يظهر شريط مستقل فوق السلايدر. فحص `node --check app.js` مرّ بدون أخطاء، وتم التأكد من وجود `clientCity` في HTML وJavaScript وقواعد إخفاء `#dueAlertBar` في CSS.

تم فتح نافذة إدارة الكليان بصرياً؛ يظهر حقل `Ville` داخل النموذج، وتظهر خانة البحث الكاملة بعرض دائري وحد ذهبي وأيقونة بحث واضحة. لم يظهر شريط التنبيه فوق السلايدر.

التحقق البرمجي النهائي أعاد: `cityField: true`، و`dueAlertDisplay: none`. كل من `.client-manager-search` و`.product-search` أصبحا بحد `2px solid rgb(217, 173, 77)` ونصف قطر `999px` وارتفاع `64px` في العرض الحالي.

تم تطبيق التصميم الجديد لخانات البحث: زوايا متوسطة بدل الشكل الكبسولي، حافة ذهبية جانبية، خلفية فاتحة مائلة للكحلي، وأيقونة ضمن مربع كحلي. فحص JavaScript استمر بالمرور دون أخطاء. أداة الفحص البصري أعادت النص والواجهة التفاعلية بنجاح، لكن لقطة الشاشة لم تتاح في هذه المراجعة.

أكد الفحص من داخل الصفحة أن خانات البحث الرئيسية وخانة الكليان أصبحت بارتفاع `58px`، ونصف قطر `16px`، وحافة يسرى ذهبية `4px solid rgb(217, 173, 77)`، وخلفية متدرجة بين الأبيض و`#f3f6fb`، مع ظل كحلي خفيف.

في اختبار وضع الآجال ظهرت الصفحة الرئيسية بعد التحميل، وتأكد بصرياً أن خانة البحث تعمل بتصميمها الجديد. سيتم الآن اختبار خيار دقيقة واحدة والتنبيه من خلال واجهة تسجيل الكوموند.

تم إنشاء كوموند تجريبي مؤقت مستحق منذ دقيقة داخل جلسة المتصفح، ثم إعادة تحميل الصفحة لاختبار ظهور التنبيه. سيتم الآن التحقق من نص التنبيه والأزرار المتاحة، ثم تنظيف بيانات الاختبار.

نتيجة الاختبار الفعلي: ظهر شريط التنبيه عند وجود كوموند تجريبي متجاوز للأجل، وظهر زر `PDF + WhatsApp · 100,00 DH`. إنشاء PDF من داخل الصفحة نجح؛ عدد السجلات المتأخرة `1`، اسم الملف `Rappel_Echeances_3D_PEINTURES_13-08-2026.pdf`، وحجم الملف `244479` بايت. لم يتم إرسال رسالة فعلية لأي زبون.

تم فتح النسخة الجديدة محلياً بعد تحديث مدة الاستخلاص؛ الصفحة حملت بنجاح ولم تظهر أخطاء واجهة قبل اختبار قوالب PDF.

اختبار PDF الفعلي نجح للخيارات الثلاثة: عند الاستلام، 15 يوماً، و30 يوماً. ظهرت النصوص الثنائية داخل البيانات الديناميكية: `إستخلاص عند الإستلام / Paiement à la livraison`، و`مدة الاستخلاص: 15 يوماً / Durée de règlement : 15 jours`، و`مدة الاستخلاص: 30 يوماً / Durée de règlement : 30 jours` مع تاريخ الاستخلاص. كما نجح PDF طلب الفاتورة، وظهرت الخيارات الأربعة داخل select.

تم فتح النسخة المحدثة محلياً وتحميل الصفحة بنجاح قبل اختبار PDF لكوموند كريدي يتضمن عدة دفعات وباقياً مستحقاً.

اختبار الكوموند الكريدي نجح: ظهرت تواريخ دفعتين بقيمتي 100 و150 DH، والباقي 250 DH، ونجح إنشاء PDF للكوموند وPDF التذكير. كما تضمن نص التذكير عبارة مسؤول الحسابات بالعربية والفرنسية. سيتم إعادة اختبار PDF الدفع عند الاستلام بشكل منفصل لأن تشغيل عدة مولدات PDF في الوقت نفسه يتنافس على عنصر المعاينة المؤقت.

اختبار PDF الدفع عند الاستلام تم بشكل منفصل ونجح: ظهر النص `إستخلاص عند الإستلام / Paiement à la livraison` دون تاريخ للاستخلاص، وتم إنشاء الملف بنجاح.

تم فتح النسخة المصححة محلياً وتحميل الصفحة بنجاح قبل اختبار مخرجات PDF للكريدي والدفع عند الاستلام بعد آخر تعديلات التصميم.

نتيجة الاختبار النهائي: في الكريدي ظهر عدد الأقساط والمجاميع بحجم صغير وعادي أحمر، وظهر الباقي بحجم كبير أحمر؛ أنشئ PDF تجريبي بنجاح. وفي الدفع عند الاستلام تم إخفاء قسم الإجمالي والأقساط وتاريخها بالكامل، وبقيت عبارة الدفع عند الاستلام فقط، وأنشئ PDF بنجاح.

تم فتح الصفحة بعد نقل `dueAlertBar` إلى نهاية `body`، وظهرت الصفحة الرئيسية والسلايدر بدون إشعار فوق السلايدر. سيتم التحقق برمجياً من ترتيب العنصر عند وجود تنبيه، ثم اختبار طريقة الأداء وPDF.

الفحص البصري الثاني أكد أن السلايدر وخانة البحث يظهران مباشرة بعد الهيدر بدون إشعار فوقهما. سيتم اعتماد فحص HTML المحلي لتأكيد أن `dueAlertBar` موجود في نهاية body لأن فحص console الأول سبق اكتمال DOM في نفس الجلسة.

اختبار المتصفح أكد خيارات طريقة الأداء الثلاثة: `نقداً / Espèces`، `شيك / Chèque`، و`كمبيالة / Cambiale`، كما أكد أن إشعار الآجال أصبح آخر عنصر داخل حاوية `.app` التي تمثل محتوى الصفحة، وليس فوق السلايدر.

اختبار PDF النهائي نجح: طريقة الأداء النقدي ظهرت بصيغة `نقداً / Espèces`، وتم إنشاء بون PDF وPDF التذكير. نص التذكير يحتوي الباقي وتاريخ انتهاء الأجل فقط، ولا يحتوي رقم الكوموند أو سجل الدفعات.

تم تحميل الصفحة بعد التعديل بنجاح، وظهر السلايدر بدون إشعار علوي. سيتم اختبار قائمة مدد الاستخلاص الثلاثية وإنشاء PDF عند الاستلام بالتنسيق المصغر الجديد.

اختبار المتصفح أكد أن قائمة مدة الاستخلاص تحتوي 3 خيارات فقط: عند الاستلام، 15 يوماً، و30 يوماً، بدون خيار التجربة. كما نجح إنشاء بون PDF عند الاستلام بالتنسيق المصغر الجديد وبحجم ملف صالح.

تم تحميل الصفحة بعد إضافة أدوات JSON بنجاح، وظهرت القائمة الرئيسية دون أخطاء. سيتم فحص عناصر قائمة الإدارة واختبار دورة تحميل/رفع ملف المنتجات.

فحص قائمة الإدارة أكد ظهور زرّي `Télécharger produits JSON` و`Importer produits JSON` داخل قسم Restoration. تم اختبار زر التحميل وظهر إشعار نجاح التحميل؛ جلسة المتصفح الحالية كانت تحتوي 0 منتوج، لذلك سيتم اختبار الاستيراد بالملف المرفق الذي يحتوي على المنتجات والصور.

ظهر الزران داخل قائمة الإدارة ونُفّذ اختبار التحميل بنجاح. محاولة رفع الملف المرفق مباشرة بالحقل المخفي لم تُقبل من بيئة المتصفح، لذلك سيُعاد الاختبار بتفعيل حقل الرفع من زر الإدارة أولاً.

تفعيل زر الرفع من قائمة الإدارة تم بنجاح، لكن أداة اختيار الملف لم تتعرف على الحقل المخفي في المتصفح. لن أعتبر ذلك فشلًا في التطبيق؛ سيتم اختبار نفس الدالة عبر جلب ملف JSON من مسار الصفحة المحلي وإنشاء File داخل المتصفح.

تم تحميل الصفحة بعد تحديث الخيارات العربية فقط، والسلايدر والواجهة الرئيسية اشتغلا بدون أخطاء. فحص HTML أكد أن مدة الاستخلاص أصبحت: عند الاستلام، 15 يوماً، 30 يوماً.

تم تحميل الصفحة بعد إعادة ترتيب الإدارة بدون أخطاء، وسيتم التحقق من القائمة للتأكد من التسلسل: 01 التسيير التجاري، 02 إدارة المنتوجات، 03 تحميل واستيراد، 04 التحليل.

تم تحميل الصفحة بعد تبسيط بون الكريدي بدون أخطاء. سيُختبر إنشاء PDF تجريبي للتأكد من حذف سجل الأقساط والباقي وظهور مجموع المنتجات وتاريخ آخر أجل والإجمالي فقط.

اختبار بون كريدي 15 يوماً نجح: ظهر مجموع ثمن المنتجات، تاريخ آخر أجل للاستخلاص، والإجمالي للبون، واختفت كلياً بلوكات سجل الأقساط والباقي وملخصات Total des acomptes. كما نجح اختبار بون الدفع عند الاستلام وبقي خالياً من ملخص الكريدي.


## اختبار نافذة تسجيل القسط وزر ملخص الزبون — 13/08/2026

تم تشغيل التطبيق محلياً على `http://127.0.0.1:4173/`، وإنشاء طلب تجريبي مؤقت بقيمة `1 200,00 DH` وأجل 15 يوماً. ظهرت نافذة تسجيل القسط بالزرين «💰 حفظ القسط» و«📲 إرسال ملخص للزبون» جنباً إلى جنب، وعند إدخال `300 DH` ظهرت المعاينة الصحيحة: مجموع الأقساط بعده `300 DH` والباقي `900 DH`.

تم اعتراض عملية المشاركة للتحقق من النص دون إرسال رسالة خارجية. الرسالة الناتجة كانت متعددة الأسطر وتضمنت الإجمالي `1 200,00 DH`، تاريخ آخر أجل `16/08/2026`، مبلغ القسط `300 DH`، والباقي بعد الأداء `900 DH`. الضغط على زر الملخص لم يسجل القسط، وأظهر إشعار نجاح، كما اجتاز `node --check app.js` فحص الصياغة.
