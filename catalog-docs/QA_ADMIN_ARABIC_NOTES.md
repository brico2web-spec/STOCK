# QA — عناوين إدارة التطبيق

تم فحص النسخة المحلية بتاريخ 2026-08-14.

ظهرت القائمة بالعناوين المطلوبة: الطلبيات، معلومات الزبون، رفع ملف الزبناء، إضافة منتوج، تعديل منتوج، حذف منتوج، وإحصائيات عامة. كما اختفت كلمة JSON من النصوص الظاهرة في القائمة، واختفى زر حفظ صورة آخر تحديث بالكامل.

تم تكبير الخطوط والمسافات داخل القائمة لتصبح القراءة أوضح على الهاتف، مع اتجاه عربي ومحاذاة يمين وعرض متجاوب. فحص `node --check app.js` نجح، كما تم التأكد من غياب `menuSaveCatalogSnapshot` و`saveCatalogSnapshot` من ملفات التشغيل.
